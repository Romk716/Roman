## Шаблон тест-кейса

- **Идентификатор:** TC-XX
- **Название:** Краткое описание проверки
- **Связь с требованиями:** R-XX
- **Связь со сценариями:** S-XX
- **Предусловия:** Что должно быть выполнено до начала теста
- **Входные данные:** Конкретные значения, которые будут использоваться
- **Шаги:** Нумерованный список действий
- **Ожидаемый результат:** Что должно произойти после выполнения шагов
- **Постусловия:** (если нужно) Действия для восстановления исходного состояния
- **Важность:** BLOCK / WARN

## Набор данных для тестирования

- **Задача A (короткая):**  
  - Название: `Smoke`  
  - Описание: `Быстрая проверка`

- **Задача B (длинная):**  
  - Название: `Отчёт по практике`  
  - Описание: `Подготовить развёрнутый отчёт о проделанной работе за первый семестр, включая графики и таблицы.`

- **Задача C (смешанный регистр):**  
  - Название: `PyTest Case`  
  - Описание: `Пример для проверки регистронезависимого поиска`

- **Ошибочный ввод D (пустое название):**  
  - Название: `""` (пустая строка)  
  - Описание: `Любое описание`

- **Ошибочный ввод E (недопустимый статус):**  
  - Статус: `отложено` (не в списке допустимых)

## TC-01: Создание задачи с валидными данными (счастливый путь)
Связь с требованиями: R-00, R-01, R-08, R-13, R-14

Связь со сценариями: S-01

Предусловия:

Файл tasks.json отсутствует или пуст.

Программа запущена в терминале с активированным окружением.

Входные данные: Задача A (название Smoke, описание Быстрая проверка).

Шаги:

Ввести команду создания задачи с указанными данными (например, add "Smoke" "Быстрая проверка").
Ввести команду просмотра списка (list).
Ожидаемый результат:

В списке отображается задача с названием Smoke, описанием Быстрая проверка, статусом по умолчанию (новая) и уникальным идентификатором (число).

Файл tasks.json создан и содержит корректную JSON-запись о задаче.

Важность: BLOCK

## TC-02: Просмотр списка после создания нескольких задач
Связь с требованиями: R-02

Связь со сценариями: S-02

Предусловия:

В системе созданы задачи A и B (например, через TC-01 и ещё одна).

Входные данные: (не требуются)

Шаги:

Ввести команду list.
Ожидаемый результат: Список содержит обе задачи с их полями.

Важность: BLOCK

## TC-03: Изменение статуса задачи на допустимый
Связь с требованиями: R-04, R-09, R-15

Связь со сценариями: S-03

Предусловия:

Существует задача A с ID, например, 1.

Входные данные: ID задачи = 1, новый статус = в работе.

Шаги:

Ввести команду изменения статуса (например, done 1 в работе или status 1 в работе).
Ввести list.
Ожидаемый результат: У задачи с ID=1 статус изменился на в работе.

Важность: BLOCK

## TC-04: Изменение статуса на недопустимый (ошибочный)
Связь с требованиями: R-05, R-15

Связь со сценариями: S-09

Предусловия:

Существует задача A с ID=1, текущий статус новая.

Входные данные: ID = 1, статус = отложено.

Шаги:

Попытаться изменить статус на отложено.
Ввести list.
Ожидаемый результат:

Программа выводит сообщение об ошибке (например, «Недопустимый статус»).

Статус задачи остаётся новая.

Важность: WARN

## TC-05: Поиск по ключевому слову (регистронезависимость)
Связь с требованиями: R-07

Связь со сценариями: S-04

Предусловия:

Созданы задачи A (Smoke) и C (PyTest Case).

Входные данные: Ключевое слово = pytest.

Шаги:

Ввести команду поиска (find pytest).
Ожидаемый результат: Найдена только задача C (PyTest Case), так как в задаче A нет слова pytest. Поиск регистронезависим.

Важность: WARN

## TC-06: Поиск по отсутствующему слову
Связь с требованиями: R-07

Связь со сценариями: (можно дополнить)

Предусловия: Созданы задачи A и B.

Входные данные: Ключевое слово = nonexistent.

Шаги:

Ввести find nonexistent.
Ожидаемый результат: Пустой список (или сообщение «ничего не найдено»).

Важность: WARN

## TC-07: Сохранение в JSON и восстановление после перезапуска
Связь с требованиями: R-08, R-10

Связь со сценариями: S-05

Предусловия:

Файл tasks.json отсутствует или пуст.

Входные данные: Задача A.

Шаги:

Создать задачу A.
Завершить программу (exit).
Запустить программу снова.
Ввести list.
Ожидаемый результат: Задача A присутствует с теми же полями.

Важность: BLOCK

## TC-08: Первый запуск без файла данных
Связь с требованиями: R-11

Связь со сценариями: S-06

Предусловия:

Файл tasks.json отсутствует.

Входные данные: -

Шаги:

Запустить программу.
Ввести list.
Ожидаемый результат: Список пуст, программа не выдаёт ошибок.

Важность: BLOCK

## TC-09: Поведение при повреждённом JSON-файле
Связь с требованиями: R-12

Связь со сценариями: S-07

Предусловия:

Создать файл tasks.json с некорректным содержимым, например, {.

Входные данные: -

Шаги:

Запустить программу.
Ввести list.
Создать задачу A.
Завершить программу и проверить содержимое tasks.json.
Ожидаемый результат:

При запуске программа не падает, список пуст.

После создания задачи файл перезаписывается корректным JSON.

Важность: BLOCK

## TC-10: Попытка создания задачи с пустым названием
Связь с требованиями: R-00, R-13

Связь со сценариями: S-08

Предусловия:

Программа запущена.

Входные данные: Пустое название (D), описание любое.

Шаги:

Ввести команду создания с пустым названием.
Ввести list.
Ожидаемый результат:

Выводится сообщение об ошибке.

Задача не создаётся, список не изменился.

Важность: BLOCK

## Анализ защиты от регрессии (для двух BLOCK-тест-кейсов)

### TC-01: Создание задачи с валидными данными
Этот тест-кейс защищает от регрессии, связанной с изменением логики создания задачи. Если при рефакторинге разработчик случайно сломает присвоение идентификатора, сохранение полей или вывод в список, тест сразу покажет несоответствие. Без него можно было бы не заметить, что новые задачи не появляются или создаются с ошибками.

### TC-07: Сохранение в JSON и восстановление после перезапуска
Данный тест гарантирует, что механизм сериализации/десериализации работает корректно. Если изменится формат JSON (например, переименуют поле `title` в `name`), тест не пройдёт, так как после перезапуска задача либо не загрузится, либо потеряет данные. Это защищает от потери пользовательских данных при обновлении версии.
